# 引き継ぎドキュメント（他セッション向け）

このファイルは、**新しいセッションが最短で文脈を把握するための入口**。
まずここを読み、必要に応じて各ファイルへ降りること。

最終更新: 2026-08-19 / DDragon patch `16.16.1`

---

## 1. このプロジェクトは何か

ユーザーは **アフェリオスOTP** のADCプレイヤー。ゴールド1からシルバー1まで落ちた時期を経て、
現在は感覚ではなく**言語化された判断基準**でプレイを立て直している。

COACH_AI としての役割・応答形式は `CLAUDE.md` を参照（日本語で応答、KRメタ必須参照など）。

---

## 2. 知識がどこにあるか

| ファイル | 役割 | 更新頻度 |
|---|---|---|
| `CLAUDE.md` | ペルソナ・応答ルール・データ取得ルール | 稀 |
| **`HANDOFF.md`**（本ファイル） | 新セッション用の入口 | 節目ごと |
| `reference/` | **静的なゲーム内数値**のプール（信頼度マーク付き） | 数値確定のたび |
| `MATCHUPS.md` | 対面別の**戦術的**対策（数値ではなく立ち回り） | 対面知識が増えるたび |
| `coaching_journal.md` | 日々のセッションログ＋コア戦略原則（時系列の全記録） | セッションごと |
| ハンドブック（Artifact） | 上記の要点を本の形にしたもの。ユーザーが読み返す用 | 節目ごと再発行 |

**役割分担の原則**: `reference/` は数値、`MATCHUPS.md` は立ち回り、`coaching_journal.md` は経緯。
同じ内容を2箇所に書かない（ズレるため）。

---

## 3. ⚠️ 数値を答える前に必ず読むもの

### 最優先: `lol-datafile` リポジトリ（別リポジトリ・自動更新）

`suoutomoteru-jpg/lol-datafile` が**権威あるデータソース**。DDragon から自動生成され、
GitHub Actions が毎月1日・15日に更新する。

```
data/champions/base_stats.md        全チャンピオンの基礎ステータス・成長値
data/champions/skills/{Id}.md       チャンピオン別スキル数値
data/items.md                       全アイテムの効果・価格
VERSION                             現在のDDragonパッチ
```

**Web検索で拾った数値より、こちらを優先すること。**（後述の通り、検索由来の数値は複数誤っていた）

このリポジトリはセッションに自動では入っていない。必要なら `add_repo` で追加するか、
`git clone https://github.com/suoutomoteru-jpg/lol-datafile.git` する。

### 次点: `reference/`

`lol-datafile` に無いもの（ルーン数値・内部クールダウン・ダメージ計算の適用順序・
アイテムパッシブの詳細）は `reference/` に手動検証してプールしてある。

### ライブ取得が必要なもの

勝率・ティアリスト・ピック率・メタ動向は時間で変わるので**キャッシュせず毎回取り直す**。

---

## 4. 🔥 ユーザーが自力で構築した判断フレームワーク

このプロジェクトの中核。**ユーザー自身が発見・言語化したもので、尊重して積み上げること。**

### 4-1. 主導権による2モード切り替え
- **勝っている（主導権あり）→ 安定度重視**: リスクを排除する保留の読み合い（KR: `홀딩` ＋ `이지선다`）
- **負けている（主導権なし）→ 奴隷の読み合い**（KR: `승부수`）: 通さなければ負ける択を取る
- 負けている時にどれだけリスキーな択を取れるかが勝敗を分ける

### 4-2. ターンの二分類
- `정상턴`（正常ターン）= 主導権から自然に発生
- `억지턴`（強制ターン）= 損失を払って無理やり作る
- **「主導権が無い＝何もできない」は錯覚**。常に①損害最小化 ②コストを払って억지턴、の二択を選んでいる

### 4-3. 受動的な安全は本当の安全ではない
「絶対にトレードを避ける」姿勢は相手に"リスクゼロで殴っていい"許可を与える。
負けていても**咎める姿勢を維持すること自体**が強気なプレイ。

### 4-4. 戦闘時間で道具を選ぶ（最重要・複数レイヤーに一般化された）
```
5秒以上殴り続けられる見込みがあるか？
  → Yes: リーサルテンポ   → No（3〜4秒）: プレスアタック
```
このフレームは**ルーン以外にも一般化されている**:
- **サモナー**: バリア＝短期バースト用 / クレンズ＝長期戦用
- **チャンピオン選択**: 敵サポートのアーキタイプが戦闘時間の予測子
  （タンク/エンゲージ型＝長い→LT、エンチャンター/バーストメイジ＝短い→PTA）

### 4-5. 「怖さ」の2分類
- ① 킬각/턴チェック不成立による**正当な慎重さ**（消してはいけない）
- ② LP不安由来の**萎縮**（克服すべき対象）
- 混同すると「もっと強気に」で誤学習する

### 4-6. 間合いは相手のCDで動的に変える
基礎値（勝てるトレード200 / イーブン650 / 負けるトレード550）を持ちつつ、
**相手のゲージクローザーがCD中なら200〜250まで詰める**。固定値に固執しない。

---

## 5. ⚠️ 過去に犯した誤りと訂正（同じ轍を踏まないため）

**Web検索由来の数値は複数回誤っていた。`lol-datafile` で必ず裏取りすること。**

| 誤り | 正しい値 | 教訓 |
|---|---|---|
| パッチを「26.13〜26.15」と表記 | **16.16**（DDragon `16.16.1`） | 2024=14.x, 2025=15.x, **2026=16.x**。検索結果の「26.x」表記に引きずられた |
| アフェリオスAD成長 `+3/Lv`（検索由来）→ `+2.3/Lv`（逆算） | **AD/Lv = 0**（成長しない） | 逆算も前提が違えば誤る。一次データを見る |
| Collector `55AD/20%クリ/脅威18` | **50AD / 25%クリ / 脅威10** | 旧パッチの数値を記憶で答えた |
| PTA「3発ごとに再発動」 | **6秒の内部CT**が律速 | クールダウンの存在を見落とした |
| PTA「相手に+12%被ダメデバフ」 | **自分に+8%の自己バフ**（再設計済み） | ルーンは大幅に作り替えられている |
| 征服者「確定ダメージ変換あり」 | **撤廃済み**。AD蓄積＋回復のみ | 同上 |
| キーストーン閾値「15秒」 | **5秒** | 12秒地点の+5.3（0.8%＝誤差）を過大評価した |
| 「タンクは防御力が高いのでPTAの固定ダメージが減衰して不利」 | **誤り**。防御力は全ダメージを同率で減衰させる。正しくは「タンクは戦闘時間を伸ばす」から | もっともらしい機序に飛びつかない |

### 運用ルール
- 数値には信頼度マークを付ける（✅確定 / ⚠️出典割れ / 🔵クライアント実測）
- ⚠️の数値を使う時は**必ずその旨を断る**
- ユーザーのクライアント実測値（🔵）が最も信頼度が高い

---

## 6. KRコミュニティ参照は必須

`CLAUDE.md` の必須要件。一度これを省略してユーザーから指摘を受けた経緯がある。
対策・数値・メタの話では毎回 KR ソース（나무위키 / 인벤 / 톡피지지 / FMKorea）を当たること。

有用だった KR 概念:
- `킬각` — スキルが落ちただけでなく、タワー/CC/フラッシュ/射程を含む総合判定で"確定的にキルできる状況"
- `주도권` と `턴` は別概念。主導権はターンに変換されて初めて価値を持つ
- `홀딩` / `이지선다` / `승부수` / `억지턴`

---

## 7. 現在の状況（2026-08-19 時点）

- **今週のフォーカス**: 「前に出る前に、ミニマップを1回見る。それだけ」
  （強気の暴走でチェックリストが機能停止し、15デス×2の日があったため、あえて1点に絞っている）
- **サブキャラ検討中**: トリスターナ（相性の補完用。移動スキル・強い序盤・dive耐性・タワー折りを埋めるが、ポーク対面だけは埋まらない）
- **忙しい時期**: メイヘム中心。集団戦のミクロだけを純粋に反復する練習台として使っている

---

## 8. 新セッションでの推奨手順

1. `CLAUDE.md` を読む（自動で読まれる）
2. **本ファイル**を読む
3. 数値の質問が来たら → `lol-datafile` → `reference/` → Web検索 の順
4. 対面の質問が来たら → `MATCHUPS.md`
5. セッションの振り返りが来たら → `coaching_journal.md` に追記（コア原則が更新されるなら冒頭のセクションも直す）
6. 節目でハンドブック（Artifact）を再発行する
